const SE = (() => {

    const error = {
        proba : 'sdfsdf'
     };

    const $_ = (value, parent = document) => parent.querySelectorAll(value);
    


    return {
        error,
        $_
    }
})();